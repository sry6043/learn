var x = "Hello";
console.log(x);  -  For output in console(Inspect).
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
if(x==undefined) : returns true if "x is undefined".
Note:
In Javascript - undefined means if x is created but has no value in it.Not defined means if x is not even declared(No x in the code).
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
String+=String; concatenates string

console.log(undefined/5) : Gives Nan(Not a Num)
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
var x='4' -string
if(x==4) -True because JS compares string and num.
if(x===4)-False ,Uses strict equality.
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
if(false||null||undefined||""||0||Nan) - All are false.
if(true&&"hello"&&1&&-1&&"false")- All are true.
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
*Note1*:
Always use curly brackets like:
fun a(){                      not like            fun b()
			{
}
			}
refer pic
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

